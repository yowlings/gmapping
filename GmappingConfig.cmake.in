# Compute paths
get_filename_component(GMAPPING_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)
set(GMAPPING_INCLUDE_DIRS "@CONF_INCLUDE_DIRS@")

# Our library dependencies (contains definitions for IMPORTED targets)
include("${GMAPPING_CMAKE_DIR}/GmappingTargets.cmake")

## These are IMPORTED targets created by FooBarTargets.cmake
#set(GMAPPING_LIBRARIES foo)
#set(GMAPPING_EXECUTABLE bar)
